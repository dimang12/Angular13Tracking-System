specVersion: "v1beta"
serviceId: "\t"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "\t-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
